{
  "核心特性": "Essential Highlights",
  "高度定制自用版": "Extreme Personal Tailored Edition",
  "FCM直连": "FCM Direct",
  "4.【特别处理】DNS 与 hosts：\n       - hosts 仅在 dns.use-hosts=true 且 dns.listen 与真正参与节点解析的 DNS 端点\n         构成闭环时改写节点 server；映射前后的域名都会纳入节点 DNS policy 匹配，\n         使私有 DNS 策略能随 hosts 的域名链路传递；": "4. [Special Handling] DNS and Hosts:\n       - hosts rewrites node servers only when dns.use-hosts=true and dns.listen forms a closed loop with the DNS endpoints actually participating in node resolution; both the pre- and post-mapping domains participate in node DNS policy matching, allowing private DNS policies to follow the hosts domain chain;",
  "hosts 仅在 dns.use-hosts=true 且 dns.listen 与真正参与节点解析的 DNS 端点 构成闭环时改写节点 server；原始域名仅用于识别并迁移私有 DNS 策略， 最终结果只保留改写后仍需 DNS 解析的节点域名 policy；": "hosts rewrites node servers only when dns.use-hosts=true and dns.listen forms a closed loop with the DNS endpoints actually participating in node resolution; the original domain is used only to identify and migrate a private DNS policy, while the final output retains policy entries only for node domains that still require DNS resolution after rewriting;",
  "此处为所有单节点（由覆写脚本自动填入订阅全部节点）": "Here are all single nodes (automatically populated by the override script with all subscription nodes)",
  "FCM：Google FCM 域名分流目标（命中规则 RULE-SET,googlefcm,FCM 后进入此组）。\n  # 模板中不隐藏该组，用于表明此代理组确实存在，是配置里真实的分流目标；\n  # 覆写脚本里设置 hidden 则是为了界面简洁：脚本借助 Bettbox 自定义脚本 UI 适配，\n  # 用 FCM直连 开关控制本组的 proxies: 字段（开=仅 DIRECT，关=仅 👉 手动切换），\n  # 方便有需要但不想直接改配置文件的用户。\n  # 开关图标取自本组的 icon 字段，换图标只需改这里一处。": "FCM: Google FCM domain traffic splitting target (routes to this group after hitting RULE-SET,googlefcm,FCM).\n  # This group is not hidden in the template to indicate that this proxy group actually exists and is a real traffic splitting target in the configuration;\n  # Hiding it in the override script is for a cleaner UI: the script leverages the Bettbox custom script UI adaptation,\n  # using the FCM Direct switch to control this group's proxies: field (on = DIRECT only, off = 👉 Manual Select only),\n  # making it convenient for users who need it without directly modifying the configuration file.\n  # The switch icon is taken from this group's icon field, and changing the icon only requires modifying it here.",
  "// Claude / Anthropic: process matches take precedence and cover Claude Desktop, Claude Code, etc.": "// Claude / Anthropic: process matches take precedence and cover Claude Desktop, Claude Code, etc.",
  "// Claude / Anthropic: official API, web login, OAuth, MCP, CLI updates, and plugin dependencies.": "// Claude / Anthropic: official API, web login, OAuth, MCP, CLI updates, and plugin dependencies."
}